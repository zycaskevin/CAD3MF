# ChatGPT Plugin / Apps SDK UI

Status: post-M0 placeholder.

Planned responsibilities:

- React + Three.js interactive model viewer
- parameter controls
- revision selector
- validation panel
- export actions
- `window.openai.callTool()` integration with the MCP server

The UI must not compile geometry itself; it consumes artifacts and invokes high-level CAD3MF tools.
